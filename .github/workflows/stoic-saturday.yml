name: Publish Stoic Saturday

# NOTE: This workflow is temporarily disabled to prevent drafts from being
# published to the live site until the publishing schedule and draft review
# process are verified.
#
# To re-enable, restore the trigger block:
#
# on:
#   schedule:
#     - cron: '0 19 * * 5'
#   workflow_dispatch:
#     inputs:
#       dry_run:
#         description: 'Dry run — do not commit changes'
#         required: false
#         default: false
#         type: boolean

jobs: {}
